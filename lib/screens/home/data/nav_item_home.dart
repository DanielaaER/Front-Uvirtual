import 'package:flutter/material.dart';

class NavItemHome extends StatelessWidget {
  const NavItemHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(""),
      )
    );
  }
}